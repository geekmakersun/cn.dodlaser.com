module.exports = {
  // 单行最大字符数设为较大值，减少不必要的换行
  printWidth: 150,
  // 缩进空格数
  tabWidth: 2,
  // 是否用制表符(tab)代替空格
  useTabs: false,
  // 语句末尾是否加分号
  semi: true,
  // 是否用单引号代替双引号
  singleQuote: true,
  // 对象/数组末尾逗号规则
  trailingComma: "es5",
  // 对象括号内是否加空格
  bracketSpacing: true,
  // 箭头函数参数是否强制加括号
  arrowParens: "always",
  // 强制标签的>与标签名称在同一行，确保闭合标签不会分开
  bracketSameLine: true,
  // HTML空格敏感度设为ignore以更好控制格式化
  htmlWhitespaceSensitivity: "ignore",
  // Vue中<script>和<style>是否缩进
  vueIndentScriptAndStyle: true,
  // 是否插入@format标记
  insertPragma: false,
  // 是否只格式化含@format的文件
  requirePragma: false,
  // Markdown换行策略
  proseWrap: "preserve",
  // 行尾换行符
  endOfLine: "lf",
  // HTML/JSX属性是否强制单行一个（true=每个属性单独一行）
  singleAttributePerLine: true,
  // 格式化嵌入的代码
  embeddedLanguageFormatting: "auto",
  // 强制HTML标签的结束括号与标签保持在同一行
  htmlSelfClosingSpace: false,
};
